include ../mixin/mixin

h3 {{.name}}

h4 All books
ul.list.pl0
  for _ in .books
    +book_item_with_tag(.)

h4 Actions
a.link.pointer.br2.ba.blue.dim.pv1.ph3(
  hx-post="/page/author/{{.name}}/rename"
  hx-prompt="New name"
  hx-target="#message"
) Rename

p#message
